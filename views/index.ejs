<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
  <div class="main w-full min-h-screen bg-zinc-900 text-white ">
    <div class="form p-10">
      <form action="/create" method="post">
        <input name="title" class="block w-full rouded-md outline-none  px-5 bg-zinc-800 py-2" type="text" placeholder="Title Here">
        <textarea name="details" class="block w-full rouded-md outline-none  px-5 bg-zinc-800 py-2 resize-none" placeholder="Write your Task Here"></textarea>
        <input class="mt-2 bg-blue-600 px-5 rounded-md py-2" type="Submit" value="Create Task">
      </form>
    </div>
    <div class="tasks flex gap-3 flex-wrap p-10">
      <% if(file.length>0){%>
      <% file.forEach(function (val) {%>
      <div class="mt-3  task w-72 rounded-md px-3 py-2 bg-zinc-800">
        <h1 class="text-3xl tracking-tighter"><%= val %></h1>
        <div class="flex w-full gap-5 justify-between mt-3
        "> 
          <a class="text-blue-600" href="/files/<%= val %>">Read More.</a>
          <a class="text-zinc-600" href="/edit/<%= val  %>">Edit</a>

        </div>
      </div>
      <%  }) %>
      <% }else{%>
      <h3 class="text-zinc-600">No Tasks Yet</h3>

      <% } %>


    </div>
  </div>
</body>

</html>